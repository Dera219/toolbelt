[pytest]
testpaths = tests
markers =
    stripe: integration tests that call Stripe's API in test mode. Require
        TOOLBELT_STRIPE_SECRET_KEY (an sk_test_ key); skipped without it.
        Deselect explicitly with: pytest -m "not stripe"
